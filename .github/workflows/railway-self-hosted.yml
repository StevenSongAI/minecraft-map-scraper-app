name: Deploy to Railway (Self-Hosted)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Deploy to Railway
        run: |
          cd $GITHUB_WORKSPACE
          railway up
