MINECRAFT MAP SCRAPER - REQUIREMENTS

1. The app must be accessible from any computer on any network via a public URL (not localhost, not local IP)

2. A user types any natural language query into a search bar and presses Enter

3. The app returns real Minecraft maps from CurseForge — not mock data, not sample data

4. Each result shows: map name, thumbnail image, download count, Minecraft version compatibility

5. Clicking a download button downloads the actual map file

6. The downloaded file is a valid Minecraft world that can be used in the game

7. If no maps match the query, the app shows a clear "no results" message

8. The app works in Safari and Chrome
